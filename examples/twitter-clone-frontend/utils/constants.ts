const COOKIE_EXPIRATION_DAYS = 7;

export const AUTH_SESSION = 'session';
export const COOKIE_EXPIRES_IN = 1000 * 60 * 60 * 24 * COOKIE_EXPIRATION_DAYS;

export const QueryFunction = 'query';
export const MutationFunction = 'mutate';
